# LTS-linear-task-solver
AI agent based on LangGraph

To start the development server, run:

```bash
bun dev
```

To bundle the app into a single file, run:

```bash
bun run build
```

After that, you can run:

```bash
bun start
```